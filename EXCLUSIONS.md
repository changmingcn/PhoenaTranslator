# 本安装包明确排除的内容

本发行包按 `SOURCE_ALLOWLIST.txt` 逐文件构建，不是从运行目录递归复制。

以下内容没有被打包：

- 既有上传文件、翻译结果、任务进度、PDF/EPUB 和页面缓存
- 日志、临时文件、备份、审计记录、测试缓存和工作流记录
- Python 虚拟环境、`__pycache__`、`.pyc`、Git 元数据
- `.env`、API Key、管理员令牌、密码、证书和私钥
- 本机字体文件及其他操作系统文件

安装时会在新系统上创建空的数据目录，并从 apt/PyPI 重新安装系统和 Python 依赖。
